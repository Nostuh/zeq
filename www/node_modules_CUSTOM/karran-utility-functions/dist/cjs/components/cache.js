"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CacheService = void 0;
const cachefactory_1 = require("cachefactory");
const cacheFactory = new cachefactory_1.CacheFactory();
class CacheService {
    constructor(name = false, options = false) {
        this.debug = false;
        try {
            if (name != false && options != false) {
                this.cache = cacheFactory.createCache(name, options);
            }
            else {
                this.cache = cacheFactory.createCache('this_cache', {
                    // Delete items from the cache when they expire
                    deleteOnExpire: 'aggressive',
                    // Check for expired items every 60 seconds
                    recycleFreq: 60 * 1000,
                    // max age -- 30 mins
                    maxAge: 30 * 60 * 1000
                });
            }
        }
        catch (e) {
            console.log(e);
            throw e;
        }
    }
    get(name, callback) {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.debug) {
                console.log("CACHE GET: " + name);
            }
            ;
            let return_data = this.cache.get(name);
            // no data mon!
            if (typeof return_data == "undefined" && typeof callback != "undefined") {
                if (this.debug) {
                    console.log('CACHE EMPTY: ' + name);
                }
                ;
                let callback_data = yield callback();
                this.set(name, callback_data);
                return callback_data;
            }
            return return_data;
        });
    }
    set(name, data) {
        if (this.debug) {
            console.log("CACHE SET: " + name);
        }
        ;
        this.cache.put(name, data);
    }
    remove(name) {
        if (this.debug) {
            console.log("CACHE REMOVE: " + name);
        }
        ;
        this.cache.remove(name);
    }
    show() {
        console.log(this.cache.info());
        return this.cache.info();
    }
    debug() {
        this.debug = true;
    }
    /**
    * @type Class
    */
    new_cache(name, options) {
        return new CacheService(name, options);
    }
}
exports.CacheService = CacheService;
