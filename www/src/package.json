{
  "name": "ki",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite --host",
    "devbuild": "vite build --watch --mode=development",
    "build": "vite build --minify=true --mode=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
